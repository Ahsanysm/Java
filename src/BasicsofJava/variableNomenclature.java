package BasicsofJava;
//calculate si
public class variableNomenclature {
    public static void main(String[] args) {
        double p,r,t,si;//4 boxes created
        p=100;
        r=10;
        t=2;
        si =(p*r*t)/100;
        System.out.println(si);
    }
}
/*Note we can use INT FLOAT or DOUBLE as our variables
because java is case sensitive so above things are different
for java compiler however int float double etc are reserved
keywords of java
 */